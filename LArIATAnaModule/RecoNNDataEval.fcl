#include "calorimetry.fcl"
#include "services_lariat.fcl"

process_name: RecoNNDataEval

services: {
    # Load the service that manages root files for histograms.
    TFileService: { fileName: "RecoNNDataEval_histo.root" }
    #@table::standard_resourcetrackers
    RandomNumberGenerator: {} #ART native random number generator
    #message:      @local::lariat_message_services
    @table::lariat_services
}
services.scheduler.SkipEvent: [ ProductNotFound ]

source: {
    module_type: RootInput
    maxEvents:   -1
}

# Define and configure some modules to do work on each event.
# First modules are defined; they are scheduled later.
# Modules are grouped by type.
physics: {
    producers: { }

    analyzers: {
        RecoNNDataEval: {
            module_type:              "RecoNNDataEval"
            Verbose:                  false
            WCTrackBuilderLabel:      wctrack
            TPCTrackHandleLabel:      pmtrack
            WC2TPModuleLabel:         wctracktpctrackmatch
            CalorimetryModuleLabel:   calo
            SimulationLabel:          largeant
            MeanDEDXNumberTrajPoints: 20
            ProtonEnergyLowerBound:   0.075
            ProtonEnergyUpperBound:   1.0
            HitsModule:               "gaushit"
            HitsInstance:             ""
            CaloAlg:                  @local::standard_calorimetryalgmc
            NNetModuleLabel:          "emtrkmichelid:emtrkmichel:EmTrackId"
            TOFModuleLabel:           tof
        }
    }

    #define the producer and filter modules for this path, order matters, 
    #filters reject all following items.  see lines starting physics.producers below

    reco: [ ]
    ana: [ RecoNNDataEval ]

    stream1: [ out1 ]

    #end_paths is a keyword and contains the paths that do not modify the art::Event, 
    #ie analyzers and output streams.  these all run simultaneously
    
    trigger_paths: [ ]
    end_paths:     [ ana, stream1 ]  
}

outputs: {
    out1: {
        module_type: RootOutput
        fileName: "RecoNNDataEval_out.root" # default file name, can override from command line with -o or --output
    }
}
